{% extends 'layout.html.twig' %}

{% block content %}
    {% if error %}
        <div class="alert alert-danger">
            Identifiants incorrects
        </div>

    {% endif %}
    <h1>Connexion</h1>
    <form method="post">
        <div class="form-group">
            <label>Pseudo</label>
            <input type="text" class="form-control" name="_username">
        </div>
        <div class="form-group">
            <label>Mot de passe</label>
            <input type="password" class="form-control" name="_password">
        </div>
        <div class="form-btn-group pull-right">
            <button type="submit" class="btn btn-primary">Se connecter</button>
        </div>
    </form>
    <p><a href="{{path('app_security_forgottenpassword')}}">Mot de passe oublié</a></p>
{% endblock %}
